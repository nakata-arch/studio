rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * 開発用ルール
     * 全てのコレクションおよびサブコレクションへの読み書きを無条件で許可します。
     * 本番環境へのデプロイ前には、必ず認証ベースのルールに戻してください。
     */
    match /{document=**} {
      allow read, write: if true;
    }

  }
}